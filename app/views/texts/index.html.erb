<%# <%= form_with(url: texts_searches_path, local: true, method: :get, class: "search-form") do |form| %>
  <%# <%= form.text_field :keyword, placeholder: "投稿を検索する", class: "search-input" %>
  <%# <%= form.submit "検索", class: "search-btn" %>
<%# <% end %> 
<div class="main-contents2">
    <% @texts.each do |text| %>
      <div class="my-parts">
        <%= link_to "詳細","/texts/#{text.id}" %>
        <div class="my-parts-body">
          <h1 class="my-parts-title"><p><%= text.title %></p></h1>
          <time><%= text.updated_at %></time>
            <span>投稿者</span><%= text.user.nickname %>
        </div>
      </div>
    <% end %>
  <%= paginate(@texts) %>
</div>